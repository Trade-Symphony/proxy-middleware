# Example environment configuration for development
# For production, use Cloudflare Workers secrets instead:
# wrangler secret put API_SERVICE_URL
# wrangler secret put API_KEY

# Development configuration (add to wrangler.jsonc vars section)
API_SERVICE_URL=http://localhost:3000
API_KEY=dev-api-key

# Example production values (use as secrets):
# API_SERVICE_URL=https://api.example.com
# API_KEY=your-production-api-key

# Setting up secrets for different environments:
# Development: Use vars in wrangler.jsonc
# Staging: wrangler secret put API_SERVICE_URL --env staging
# Production: wrangler secret put API_SERVICE_URL --env production
